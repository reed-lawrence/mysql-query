{
  "name": "mysql-query",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node scripts/clean-dist.js && tsc",
    "build:prod": "npm run build && node scripts/pre-publish.js",
    "publish:release": "npm run build:prod && cd ./dist && npm publish --access public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/reed-lawrence/mysql-query.git"
  },
  "keywords": [
    "mysql"
  ],
  "author": "Reed Lawrence",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/reed-lawrence/mysql-query/issues"
  },
  "homepage": "https://github.com/reed-lawrence/mysql-query#readme",
  "dependencies": {
    "mysql": "^2.18.1"
  },
  "devDependencies": {
    "@types/mysql": "^2.15.15",
    "fs-extra": "^9.0.1"
  }
}
